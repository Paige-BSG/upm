# KubeBlocks vendor home — source summary

**COMPARISON BASELINE** — an external open-source project UPM compares against. Not UPM
tech stack, and not a component UPM depends on.

- Raw record: `llm-wiki/raw/sources/kubeblocks-home/dc9e79d453e59f58abde8e3c65bbe5496477ef5a1f96be0884ad4de90127df49.json`.
- Upstream: <https://kubeblocks.io/> — the vendor site for
  [apecloud/kubeblocks](https://github.com/apecloud/kubeblocks).

## What it is

KubeBlocks is a Kubernetes API to run databases and middleware. Its vendor home page
positions it as an Open Source project with a separately-tiered Enterprise offering
("Open Source + 50+ Enterprise Customers").

## Notes

- The vendor home is dynamic; the record carries a timezone-aware `retrievedAt` and the
  ETag from that fetch so a later change is re-ingested as a new record.
- The site is the comparison baseline for how UPM frames its own openness. It is used to
  state the factual OSS / Enterprise boundary, not to editorialize the vendor's business
  model. See [comparisons/kubeblocks](comparisons/kubeblocks.md).
- The underlying repository's license is declared in the upstream repo; this record
  fingerprints the vendor site, not the repo source.
